% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusters.R
\name{getHostCombination}
\alias{getHostCombination}
\title{Pick how many workers to run on each host}
\usage{
getHostCombination(outs, Npops = 100)
}
\arguments{
\item{outs}{A list of \code{\link[=testMachine]{testMachine()}} results, one per host, named by host.}

\item{Npops}{Integer; total number of workers to allocate.}
}
\value{
A \code{data.table} with one row per host, giving the workers assigned and
the predicted time.
}
\description{
Takes the per-host timings from \code{\link[=runTests]{runTests()}} and fits, for each host, a
linear model of the slowest worker's time against the number of workers. The
models are then used to choose an allocation of \code{Npops} workers across the
hosts that finishes soonest, since a host whose per-worker time degrades
quickly is worth fewer workers than its core count suggests.
}
