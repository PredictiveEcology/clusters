% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/physicalCores.R
\name{.physicalCores}
\alias{.physicalCores}
\title{Number of physical cores on this machine}
\usage{
.physicalCores(sysfs = "/sys/devices/system/cpu", cpuinfo = "/proc/cpuinfo")
}
\arguments{
\item{sysfs, cpuinfo}{Paths to the Linux CPU topology directory and to \code{cpuinfo}; arguments only so
tests can point them at fixtures.}
}
\value{
Integer number of physical cores, or \code{NA} if it cannot be determined.
}
\description{
\code{\link[=.ht_allocate_min]{.ht_allocate_min()}} gives a host's real cores full weight and its hyperthreads less, so it needs
the physical count. \code{parallel::detectCores(logical = FALSE)} does not give it on Linux: it returns
the logical count there. On Linux this reads the kernel's CPU topology instead: each logical CPU
lists the logical CPUs it shares a core with (\code{thread_siblings_list}), so the number of distinct
lists is the number of cores. \verb{/proc/cpuinfo} (distinct \verb{physical id} x \verb{core id}) is the fallback.
Elsewhere (macOS, Windows) \code{detectCores(logical = FALSE)} is used.
}
\keyword{internal}
