% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tracking.R
\name{monitorCluster}
\alias{monitorCluster}
\title{Watch active threads and memory across a cluster's hosts}
\usage{
monitorCluster(cl, cores, pad = 2, interval = 1)
}
\arguments{
\item{cl}{A running cluster with one worker per host, in the same order as
\code{cores}. If missing, one is built over SSH from \code{cores} and stopped on exit.}

\item{cores}{Character vector of host names, in the cluster's worker order.}

\item{pad}{Integer; spaces between columns.}

\item{interval}{Numeric; seconds between polls.}
}
\value{
Invisibly, a list with \code{threads} (peak active threads per host) and
\code{ram} (peak memory used per host, GB). Returned on interrupt.
}
\description{
Polls every worker for its active-thread count and its memory use, and
redraws two aligned rows in place: threads on the first, \code{used/totalGB} on
the second. Interrupt it (Ctrl-C) to stop; it then prints the per-host peaks
it saw and returns them.

Neither probe needs \code{clusters} installed on the hosts. The functions are
re-homed in the global environment before being sent, so they travel whole
rather than as a reference to this package's namespace.
}
\note{
The display uses ANSI erase sequences to redraw in place. Redirect it
to a file and you get one block per tick instead of a live display.
}
\examples{
\dontrun{
hosts <- c("birds", "biomass", "camas")
peaks <- monitorCluster(cores = hosts)   # Ctrl-C to stop
peaks$ram
}
}
