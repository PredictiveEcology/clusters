% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusterSetup.R
\name{readTransferredObjects}
\alias{readTransferredObjects}
\title{Read objects transferred to a worker, failing loudly if the transfer did not arrive}
\usage{
readTransferredObjects(therePath, filenameForTransfer)
}
\arguments{
\item{therePath}{Path to the worker-local copy of the transferred objects.}

\item{filenameForTransfer}{Path to the original, used as a fallback.}
}
\value{
The list of transferred objects.
}
\description{
Tries the local (rsynced) copy first, then the original shared path. A failed read used to fall
through to \code{\link[reproducible:.unwrap]{reproducible::.unwrap()}} on a \code{try-error}, which produced objects whose terra
external pointers were dangling; the job then died much later with
\code{"external pointer is not valid"}, an error saying nothing about a file transfer. Verifying here
keeps the error where the cause is.
}
