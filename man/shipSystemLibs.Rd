% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/systemLibs.R
\name{shipSystemLibs}
\alias{shipSystemLibs}
\title{Ship missing system shared libraries to worker hosts, without root}
\usage{
shipSystemLibs(cl, hosts, libs, dest = "~/.local/lib/clusters", verbose = TRUE)
}
\arguments{
\item{cl}{A running cluster with one worker per host (the probe cluster).}

\item{hosts}{Character vector of host labels, in the same order as \code{cl}.}

\item{libs}{Library path(s) whose compiled code should be checked.}

\item{dest}{Directory on each host to copy into. Must be user-writable;
defaults to \verb{~/.local/lib/clusters}.}

\item{verbose}{Logical; report what is shipped.}
}
\value{
Invisibly, a named list with \code{dest} (as given), \code{destResolved} (the
absolute directory on each host), \code{ldPath} (each host's existing
\code{LD_LIBRARY_PATH}), \code{shipped} (per host, the sonames copied) and \code{unmet}
(per host, anything still missing that this will not ship).
}
\description{
The project library is rsynced to each host, but the \emph{system} libraries those
compiled packages link against are not. A host missing one fails at load with
e.g. \verb{libtbb.so.12: cannot open shared object file} -- RcppParallel, and so
everything that depends on it. The obvious fix, \verb{sudo apt-get install}, needs
root on every host, which is exactly what a shared research cluster tends not
to give you.

It is not needed. A shared library is just a file: copy it into a
user-writable directory on the host and point \code{LD_LIBRARY_PATH} at it. This
detects what is actually missing, ships only that, and returns the directory
so the caller can add it to the workers' environment.
}
\section{What is deliberately not shipped}{

The core runtime -- \code{libc}, \verb{libstdc++}, \code{libgcc_s}, \code{libm}, \code{libpthread},
\code{libdl}, \code{librt} and the loader itself. Those must be the host's own: they are
what everything else on the machine is already linked against, and overriding
them via \code{LD_LIBRARY_PATH} breaks far more than it fixes. If one of those is
genuinely missing, the host is not usable and should be reported, not patched.
}

\section{When this refuses}{

Only when the master and the host agree on architecture and libc version is
copying a binary between them sound. Hosts that disagree are skipped with a
message rather than given a library that cannot load, or worse, one that can.
}

\section{Setting \code{LD_LIBRARY_PATH}}{

It must be in the worker's environment \strong{before R starts}. glibc parses
\code{LD_LIBRARY_PATH} once at process startup, so \code{Sys.setenv()} from inside R --
which is what \code{parallelly}'s \code{rscript_envs} does, via
\verb{Rscript -e 'Sys.setenv(...)'} -- has no effect on later \code{dlopen()}. Verified:
with the library shipped but only \code{Sys.setenv()}, \code{library(fireSenseUtils)}
still fails; with \verb{env LD_LIBRARY_PATH=... Rscript}, it loads. Prefix the
worker command instead.
}

