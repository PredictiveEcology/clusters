% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verifyHosts.R
\name{verifyClusterHosts}
\alias{verifyClusterHosts}
\title{Verify that every cluster host can actually run a worker}
\usage{
verifyClusterHosts(cl, hosts, pkgs, libs, action = c("stop", "drop"))
}
\arguments{
\item{cl}{A running cluster with one worker per host (the probe cluster).}

\item{hosts}{Character vector of host labels, in the same order as \code{cl}.}

\item{pkgs}{Character vector of packages that a worker must be able to load.}

\item{libs}{Library paths to prepend on each worker (the master's).}

\item{action}{\code{"stop"} (default) to error listing the bad hosts, or \code{"drop"}
to return only the hosts that passed.}
}
\value{
Character vector of hosts that passed, invisibly. With
\code{action = "stop"} that is always \code{hosts}.
}
\description{
\code{plan_psock_min()} rsyncs the master's library to the same absolute path on
each host and prepends it. Three things can silently go wrong with that, and
each produces a worker that dies later rather than a build that fails now:
\itemize{
\item \strong{R version drift.} A library built under one R major.minor is ignored by
another, which then falls back to the system library and cannot find the
packages at all. (\code{plan_psock_min()} has always checked this one.)
\item \strong{Package version drift.} The rsync only carries the dependency closure of
\code{pkgsNeeded}, so anything outside it can differ between master and worker --
including the packages doing the orchestrating.
\item \strong{A library that is present but unloadable}, typically a missing system
library (\code{libtbb.so.12} for RcppParallel, \code{libgdal.so.NN} for sf/terra) or a
binary built against a different GDAL.
}

This checks all three against the master and reports every offending host at
once, rather than failing on the first or -- worse -- succeeding and letting
the workers die one at a time inside the objective function.
}
