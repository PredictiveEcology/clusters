% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusterSetup.R
\name{makeClusterPSOCK}
\alias{makeClusterPSOCK}
\title{Lightweight wrapper for parallelly::makeClusterPSOCK}
\usage{
makeClusterPSOCK(
  workers,
  outfile = NULL,
  rscript_libs = .libPaths(),
  ...,
  rscript = NULL,
  default_packages = c("datasets", "utils", "grDevices", "graphics", "stats", "methods"),
  port = NULL,
  rshopts = .sshKeepaliveOpts(c("-o", "ForwardX11=no", "-o", "ExitOnForwardFailure=yes")),
  tries = 5L,
  delay = 5,
  renice = 20,
  revtunnel = TRUE
)
}
\arguments{
\item{workers}{The hostnames of workers (as a character vector) or the
number of localhost workers (as a positive integer).}

\item{outfile}{Optional log file path.}

\item{rscript_libs}{Optional library paths for workers.}

\item{...}{Additional arguments passed to parallelly::makeClusterPSOCK.}

\item{rscript}{The command that starts a worker, passed to \code{\link[parallelly:makeClusterPSOCK]{parallelly::makeClusterPSOCK()}}. It
is an argument here, not left to \code{...}: otherwise \verb{rscript =} partially matched \code{rscript_libs}
and became the workers' library path whenever \code{rscript_libs} was not also given.}

\item{default_packages}{Packages R attaches in each worker; the default is parallelly's.}

\item{port}{Optional port or port block start.}

\item{rshopts}{Character vector of options passed to \code{ssh}. The defaults disable X11 forwarding and fail fast when a tunnel cannot be established.}

\item{tries, delay}{Maximum number of attempts done to launch each node
with \code{makeNode()} and the delay (in seconds) in-between attempts.
If argument \code{port} specifies more than one port, e.g. \code{port = "random"}
then a random port will be drawn and validated at most \code{tries} times.
Arguments \code{tries} and \code{delay} are used only when
\code{setup_strategy == "sequential"}.}

\item{renice}{A numerical 'niceness' (priority) to set for the worker
processes.}

\item{revtunnel}{If TRUE, a reverse SSH tunnel is set up for each worker such
that the worker \R process sets up a socket connection to its local port
\code{(port + rank - 1)} which then reaches the master on port \code{port}.
If FALSE, then the worker will try to connect directly to port \code{port} on
\code{master}.
If NA, then TRUE or FALSE is inferred from inspection of \code{rshcmd[1]}.
For more details, see below.}
}
\description{
Lightweight wrapper for parallelly::makeClusterPSOCK
}
\details{
A worker command that starts with \code{env} (the OpenBLAS cap from \code{\link[=.workerRscript]{.workerRscript()}}, or an
\code{LD_LIBRARY_PATH} prefix) is not recognised by parallelly as Rscript, so parallelly passes the
default packages as an \verb{R_DEFAULT_PACKAGES=} assignment in front of the command. With \code{renice}
that assignment ends up after \code{nice}, which then fails to run it ("nice:
'R_DEFAULT_PACKAGES=...': No such file or directory"). For such a command the default packages
are put among \code{env}'s own assignments instead.
}
