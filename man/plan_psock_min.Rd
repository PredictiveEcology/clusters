% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimateCluster.R
\name{plan_psock_min}
\alias{plan_psock_min}
\title{Minimal HT-aware PSOCK cluster planner (parallelly-only)}
\usage{
plan_psock_min(
  hosts,
  total = 100L,
  beta = 0.5,
  load_memory = "5min",
  fraction = 0.9,
  pkgsNeeded = c("parallelly", "future", "foreach"),
  user_lib_template = "~/.local/R/\%v/library",
  install_repos = c("https://cloud.r-project.org", "https://cran.r-project.org"),
  rshopts = c("-T", "-o", "ConnectTimeout=10"),
  rscript = "Rscript",
  auto_stop = TRUE,
  logPath = NULL,
  libPath = NULL,
  build_final_cluster = TRUE
)
}
\arguments{
\item{hosts}{Character vector of hostnames reachable by SSH.}

\item{total}{Integer total desired workers (default 100).}

\item{beta}{Numeric in (0,1], HT penalty beyond 50\% logical occupancy (default 0.5).}

\item{load_memory}{Character; \code{"1min"|"5min"|"15min"} window for \code{freeCores()} (default \code{"5min"}).}

\item{fraction}{Numeric ≥ 0; headroom scale for \code{freeCores()} (default 0.9).}

\item{pkgsNeeded}{Character vector of packages to ensure on workers
(default \code{c("parallelly","future","foreach")}).}

\item{user_lib_template}{Character; per-user library path template; \verb{\%v} expands to R version
(default \code{"~/.local/R/\%v/library"}).}

\item{install_repos}{Character vector of CRAN-like repos tried in order
(default \code{c("https://cloud.r-project.org","https://cran.r-project.org")}).}

\item{rshopts}{Character vector of SSH options passed by parallelly (default \code{c("-T","-o","ConnectTimeout=10")}).}

\item{rscript}{Character; path to \code{Rscript} on workers (default \code{"Rscript"}).}

\item{auto_stop}{Logical; auto-stop clusters on GC (default \code{TRUE}).}

\item{logPath}{Optional local file to append compact bootstrap / probe info.}

\item{libPath}{Optional site library path to prepend and use if writable.}

\item{build_final_cluster}{Logical; if \code{TRUE} launch the final cluster (default \code{TRUE}).}
}
\value{
A list with:
\itemize{
\item \code{probe}: per-host capacity & load,
\item \code{allocation}: per-host assignments (HT-aware),
\item \code{workers}: hostname vector repeated per assigned workers,
\item \code{total_requested}, \code{total_assigned},
\item \code{cluster} (if built).
}
}
\description{
Launches a probe cluster (1 worker per host) via \code{\link[parallelly:makeClusterPSOCK]{parallelly::makeClusterPSOCK()}}
using \strong{reverse SSH tunneling} (\code{revtunnel = TRUE}) and \strong{sequential} setup (on Rstudio),
\strong{parallel} otherwise,
configures a \strong{per-user library} on each worker (created via \code{dir.create(recursive=TRUE)}),
installs & loads \code{pkgsNeeded} in that user library, measures per-host capacity via
\code{\link[parallelly:availableCores]{parallelly::availableCores()}} and \code{\link[parallelly:freeCores]{parallelly::freeCores()}}, performs a
\strong{hyper-threading-aware} allocation of up to \code{total} workers (β penalty beyond ~50\% logical
occupancy), and optionally launches the final cluster with the computed worker counts.

This is intentionally minimal—no \code{master} argument, no diagnostics or fallbacks.
}
\examples{
\dontrun{
plan <- plan_psock_min(
  hosts = c("nodeA","nodeB","nodeC","nodeD"),
  total = 100,
  pkgsNeeded = c("parallelly","future","foreach"),
  user_lib_template = "~/.local/R/\%v/library",
  build_final_cluster = TRUE
)
plan$allocation
parallel::stopCluster(plan$cluster)
}

}
\references{
parallelly PSOCK cluster setup & worker startup controls:
https://parallelly.futureverse.org/reference/makeClusterPSOCK.html
availableCores / freeCores:
https://cran.r-project.org/web/packages/parallelly/parallelly.pdf
https://rdrr.io/cran/parallelly/man/freeCores.html
Base parallel PSOCK notes (background):
https://web.mit.edu/r/current/lib/R/library/parallel/html/makeCluster.html
}
