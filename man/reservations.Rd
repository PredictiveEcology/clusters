% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reservations.R
\name{reservationsPath}
\alias{reservationsPath}
\alias{liveReservations}
\alias{reserveCores}
\alias{releaseCores}
\title{Core reservations shared between concurrent cluster builds}
\usage{
reservationsPath(path = getOption("clusters.reservationsPath"))

liveReservations(path = getOption("clusters.reservationsPath"))

reserveCores(
  alloc,
  id = basename(tempfile("resv")),
  pid = Sys.getpid(),
  path = getOption("clusters.reservationsPath")
)

releaseCores(
  id = NULL,
  pid = Sys.getpid(),
  path = getOption("clusters.reservationsPath")
)
}
\arguments{
\item{path}{Directory holding the ledger. Defaults to
\code{getOption("clusters.reservationsPath")}, else a per-user data directory.}

\item{alloc}{A data.frame with columns \code{host} and \code{assign}, as returned in the
\code{allocation} element of \code{\link[=plan_psock_min]{plan_psock_min()}}.}

\item{id}{Reservation id. One process may hold several clusters at once, so
releases are keyed by reservation rather than by process.}

\item{pid}{Owning process id; defaults to this process.}
}
\value{
\code{reservationsPath()} a file path; \code{liveReservations()} a data.frame
with columns \code{id}, \code{pid}, \code{host}, \code{workers}, \code{created}; \code{reserveCores()}
returns its reservation \code{id}; \code{releaseCores()} returns invisibly.
}
\description{
\code{plan_psock_min()} sizes a cluster from \code{\link[parallelly:freeCores]{parallelly::freeCores()}}, which reads
a \emph{trailing} load average (5 minutes by default). Two clusters built inside
that window therefore both observe the same idle cores and both claim them:
the load produced by the first has not yet reached the average the second
reads. Staggering the builds by more than the averaging window papers over
this; it does not fix it, and it forces callers to hand-tune both the stagger
and the number of concurrent jobs against the size of the fleet.

These functions keep a small ledger of what has actually been handed out, so
allocation is based on \emph{reservations} rather than on a lagging measurement.
Each successful build records \code{host -> workers} under a reservation id owned
by the building process; \code{\link[=freeCoresLessReserved]{freeCoresLessReserved()}} subtracts those from the
measured free cores. Releases are keyed by id, because one process may hold
several clusters at once.

The ledger is self-healing: every read drops entries whose owning process is
no longer alive, so a master that is killed without releasing cannot leak a
reservation forever.
}
\section{Scope}{

The ledger is a file, so it is shared by every process that can see that file
-- which is what is needed when several worker panes on one machine each build
their own cluster over a shared fleet. If \emph{different} machines build clusters
over the same fleet, point them all at one path on shared storage via
\code{options(clusters.reservationsPath = )}.
}

