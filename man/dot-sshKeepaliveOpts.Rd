% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimateCluster.R
\name{.sshKeepaliveOpts}
\alias{.sshKeepaliveOpts}
\title{SSH options that let the master notice a worker has died}
\usage{
.sshKeepaliveOpts(extra = NULL, interval = 30L, countMax = 4L)
}
\arguments{
\item{extra}{Character vector of additional SSH options to append.}

\item{interval}{Seconds between keepalive probes.}

\item{countMax}{Number of unanswered probes before SSH closes the connection.}
}
\value{
A character vector of SSH options for \code{rshopts}.
}
\description{
PSOCK workers are reached over SSH, and the master reads results with \code{unserialize()} on the
socket. That read is bounded by parallelly's \code{timeout}, which we set to 30 days so that a
legitimately long computation is never cut off. The consequence is that if a worker's R process
dies \emph{after} connecting, SSH holds the tunnel open, the socket never closes, and the master
blocks for up to 30 days -- in practice, forever.
}
\details{
\code{connectTimeout} does not help: it only bounds the initial connect, which already succeeded.

\code{ServerAliveInterval}/\code{ServerAliveCountMax} close the door from the SSH side instead. SSH probes
an idle connection every \code{ServerAliveInterval} seconds and tears it down after
\code{ServerAliveCountMax} unanswered probes, so a dead worker surfaces as a closed connection and an
R error within ~2 minutes rather than hanging the run. A busy worker answers the probes, so long
computations are unaffected -- the probes run at the SSH layer, not in R.

Observed 2026-09-19/20: two re-scores hung for hours after a worker died at startup with
\verb{Error in unserialize(node$con)} inside \code{workRSOCK}, and the masters had to be killed by hand.
}
